# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages

after_success:
  - travis_wait 100 Rscript -e 'covr::codecov()'

r_github_packages:
  - rorynolan/filesstrings
  - rorynolan/ijtiff
